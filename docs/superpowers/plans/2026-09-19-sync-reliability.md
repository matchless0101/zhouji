# 云同步与运维修复实施计划

> 使用测试驱动开发完成修复；后端和运维已独立复核，iOS 由主控完成最终复核。真机调试由用户执行。

**目标：** 修复并发修改误判和清理后旧数据复活，补齐可验证的备份、清理、健康定时任务与恢复演练。

**方案：** 保持现有 API 形状，同版本仅在内容、操作和时间均一致时视为重试。七天后删除正文，单独保存无正文删除凭据及序号；账户行锁串行化推送与清理。运维脚本失败必须返回非零，备份数据库及匹配密钥，在隔离库验证恢复。

**约束：** 不增加第三方依赖；Swift 6 / SwiftUI / SwiftData / iOS 17；同步默认关闭；不操作真实用户内容或登录；保留工作区已有图片；用户已授权修复，按 AGENTS.md 验证后提交并推送当前跟踪分支。

**依据：** `docs/05-云同步产品需求增量.md` R1/R2、`docs/08-云同步部署与真机联调.md` 与 2026-09-18 隔离复现。

## 1. 同步协议与客户端

- [x] 在 `server/tests/test_sync.py` 增加同版本异内容冲突、真正重试、清理后旧编辑拒绝、游标不倒退及真实双账户隔离测试，先运行并确认失败。
- [x] 修改 `server/src/zhouji_api/sync.py`、`models.py`、`auth.py`；新增 `005_sync_tombstones.sql`。删除凭据只含账户、实体标识、版本、序号与删除时间，注销一并删除。
- [x] iOS 接收无正文删除冲突时只标记已有本机实体删除，保留历史字段及计时快照；补 `ContentSyncStoreTests.swift`。同时修复内容变更漏检、同步响应期间的新编辑漏报和永久删除冲突反复上传。
- [x] 后端全量测试、iOS 相关测试与模拟器构建；后端独立复核及 iOS 主控复核。

## 2. 运维脚本

- [x] 回归证明 dump 失败不能留下成功备份；完善权限、原子产出、配置与密钥同批打包。
- [x] 新增定时备份、七天清理、健康和备份过期监控的 systemd 单元；不假称已接入外部通知。
- [x] 在临时目录验证脚本故障路径与成功路径，检查 shell 与单元语法；独立复核发现的包完整性、维护连接覆盖问题已修复并验证。

## 3. 部署、演练与交付

- [x] 生产变更前完成全量备份；仅对粥记执行新增表迁移，发布到独立版本目录，可回退旧服务。
- [x] 安装定时任务并执行一次；隔离库恢复备份，核对各表数量、密钥可解密性与健康状态，不覆盖生产。
- [x] 核验公网 ready=200、无登录同步=401、线上源码摘要与本地一致。
- [x] 更新部署、备份与现状文档，明确外部告警/异地备份配置和用户真机验收边界。
- [x] 完成交付差异检查，提交范围排除既有未跟踪图片；提交及推送结果以 Git 记录为准。

验证记录：服务端本地 71 项通过、1 项跳过，服务器含隔离 MySQL 共 72 项通过；iOS 76 个单元测试通过（参数化 84 次），同步专项 12 项包含其中，界面回归 17 项通过，模拟器构建通过。部署和恢复演练证据见 `docs/08-云同步部署与真机联调.md`。

开放前仍待完成：用户真机验收；异地备份位置、外部告警渠道及正式备份保留期/RPO/RTO 确认与配置；开放范围与隐私说明更新。同步默认开关保持关闭。

# 云同步部署与真机联调清单（阶段 E）

_在打开用户可见同步之前，按顺序完成本清单。每项写明执行人、时间与结果；未通过不得打开开关。_

## 2026-09-20 可靠性修复与部署验收

本次已完成并部署服务端修复：同版本不同内容返回冲突、真正重试去重、七天清理后保留无正文删除凭据、清理不回退同步序号、注销清除删除凭据、删除数量正确统计。迁移为 `005_sync_tombstones.sql`，必须先迁移再发布 API。

已部署完整备份包、每日备份/清理和每五分钟健康/备份过期检查的脚本与 systemd 单元。故障目前记录在服务器 systemd/journal，外部通知尚未配置。

- [x] 上传修复版并完成隔离 MySQL 并发验证
- [x] 生产变更前完整备份，执行 005，切换修复版
- [x] 安装并核验三个定时任务
- [x] 完整备份导入隔离库，核对表数量及匹配密钥
- [ ] 配置异地备份、外部故障通知及正式保留期（待用户提供位置/渠道）
- [ ] 用户自行完成下方单设备、双设备、注销与异常真机验收

历史核查（2026-09-18/19）：当时生产运行 `content-sync-20260914-01`；健康接口 200、未登录同步 401。仅找到 9 月 14 日旧备份；虽然健康探针文件存在，未发现粥记定时任务。旧部署记录不能代替当前运行证明。

## 本轮实际验证记录

- 生产版本：`/opt/zhouji-api/releases/sync-reliability-20260920-01`；005 表迁移完成，API active；公网 ready=200、无登录同步=401。
- 服务端：本地 71 项通过，1 项真实 MySQL 测试仅在服务器启用；服务器 72 项全部通过，含随机隔离库并发测试。已有两条测试依赖弃用提示不影响结果。
- iOS：76 个单元测试通过（参数化执行共 84 次）；12 个同步专项包含其中；通用模拟器构建通过。2026-09-20 完整模拟器界面回归 17 项全部通过。真机未操作。
- 生产变更前完整备份：`zhouji-20260920-193700.bundle.tar.gz`。
- 恢复成功包：`zhouji-20260920-193921.bundle.tar.gz`，权限 600；向随机隔离库恢复耗时 0.232 秒（本次小样本，不作为正式 RTO 承诺）。
- 恢复/当前库数量一致：`auth_accounts=2`、`auth_challenges=1`、`auth_sessions=1`、`sync_entities=0`、`sync_tombstones=0`。使用包内匹配密钥成功解密 2 个既有加密令牌，只记录结果、不输出令牌。隔离库已删除，生产未被恢复覆盖。
- 首次使用较早备份验证时表数量与当前库不同，检查拒绝通过；重新生成最新备份后通过。没有将旧备份覆盖生产。
- 三个 service 手动执行均 `Result=success / ExecMainStatus=0`，三个 timer 已启用。恢复包完整性检查包含外层 gzip、固定五文件、非空及内层 SQL gzip。
- 服务端内容同步源码 SHA256：`2060180eb063b51254d76057edc3ca4da2269d5acb601f624d74f06c9d215e75`，线上与本地相同。

2026-09-22 收尾复核：本地服务端再次通过 71 项（真实 MySQL 项本地跳过）；API 与三个 timer 均 active，三个 service 最近执行均成功。9 月 21 日 03:22、22 日 03:20 已自动生成完整备份包，权限均为 600；公网 ready=200。后端和运维代码已独立复核，提出的问题已修复并验证；iOS 改动由主控完成最终复核。恢复演练脚本已同步到部署目录，异常诊断不输出凭据。

真机重点补验本轮修复：任务改名、取消完成和撤销删除后能同步；同步期间继续编辑时仍提示新变更待同步；双设备同版本不同内容出现冲突。七天清理后的冲突提供“保留本机副本”和“接受云端删除”，前者应持续显示仅存本机且不再反复上传，后者保留历史计时快照。清理边界可使用专用测试账户准备数据，不对真实用户内容做实验。

新 release 复用旧 release 的虚拟环境依赖，通过 `PYTHONPATH=/opt/zhouji-api/current/src` 加载当前源码；清理旧 release 前必须迁移该依赖目录。回退 API 时保留新增表与备份，先停清理 timer，再切回 `content-sync-20260914-01` 并重启；旧版重新带回本轮修复前的问题，不能据此开放同步。

## 0. 前提（代码侧已具备）

| 项 | 状态 |
| --- | --- |
| R1–R5 决策 | 已确认 |
| 服务端 `sync_entities` + push/pull/status | 已实现（`004_content_sync.sql`） |
| iOS 上传/恢复/增量/冲突 | 已实现，**默认关闭** |
| 本机分库与 JSON 备份 | 已实现 |

开启测试：真机 Scheme 增加启动参数 `-ZJSyncContent`，或设置 `UserDefaults` 键 `contentSync.enabled=true`。正式发布打开前须完成本清单并单独提交。

---

## 1. 服务器部署（2026-09-14 已执行）

- [x] 备份当前 `zhouji` 库 → `/var/backups/zhouji/zhouji-20260914-160830.sql.gz`（zhouji_app 无 PROCESS 权限，dump 含 3 张 auth 表；生产灾备建议改用 debian-sys-maint 全量）
- [x] 对 `zhouji` 执行 `migrations/004_content_sync.sql`（TEXT 默认值已改为无 DEFAULT 以适配 MySQL 8 严格模式）
- [x] 部署 `/opt/zhouji-api/releases/content-sync-20260914-01`，`current` 已切换；`systemctl restart zhouji-api`
- [x] 原“每 5 分钟 cron”记录未通过核验；2026-09-20 已用 `zhouji-ops-check.timer` 安装并验证每五分钟检查
- [x] 未登录 `/api/v1/sync/pull` 返回 401；公网 ready=200
- [x] 服务器侧 pytest 50 项通过
- [ ] 登录后 `/sync/status` 返回 `latest_seq=0`（待真机测试账户联调时确认）
- [x] 2026-09-20 通过 `zhouji-purge.timer` 每日执行七天清理，已验证退出码 0

## 2. 单账户真机联调（测试账户）

设备 A，登录测试账户，开启 `-ZJSyncContent`：

- [ ] 「我的」出现「云同步（测试）」
- [ ] 创建任务 + 结束一段计时 →「同步变更」成功
- [ ] 运行中计时**不会**出现在上传结果中（R3）
- [ ] 再点「同步变更」提示无新变更或 0 项
- [ ] 登出再登录，「从云端恢复」后任务与已结束计时仍在
- [ ] 断网后点同步：失败可重试，本机数据完整

## 3. 双设备

设备 A 与 B 同一测试账户：

- [ ] A 上传后 B 恢复，内容一致
- [ ] A 改标题并同步 → B 恢复/同步后标题更新
- [ ] 两端同时改同一任务并都同步 → 后写端出现冲突
- [ ] 选「保留本机」→ 云端变为本机内容；选「使用云端」→ 本机变云端内容
- [ ] 一端软删除任务并同步 → 另一端恢复后列表不出现该任务，历史计时快照仍在

## 4. 注销闭环

- [ ] 账户有已同步内容 → 注销成功
- [ ] 服务端该账户 `sync_entities` 行数为 0（或账户行不存在）
- [ ] 本机账户库备份仍可导出；重新登录后云端为空（需再次上传）
- [ ] 注销失败时可重试，不假装成功

## 5. 文案与边界

- [ ] 关闭同步时：登录页仍显示「云同步尚未上线 / 数据仍在本机」
- [ ] 打开同步后：上传成功文案为「已上传 N 条」，**不**写「备份完成」
- [ ] 满额（>5000）提示「需要完整对账」，仍可新建任务与导出
- [ ] 游客库内容不会出现在账户上传确认范围说明中

## 6. 开放开关（最后一步）

仅当 1–5 全部勾选并归档证据：

- [ ] 产品负责人书面确认开放范围（全体用户 / 白名单）
- [ ] 提交将默认开关改为开启的独立 commit（或服务端功能开关）
- [ ] 更新 README / PRD「当前阶段」与隐私说明中的数据类别
- [ ] 观察期告警：`health-probe` 失败、push 5xx 比例

---

## 验收记录模板

| 日期 | 执行人 | 设备/系统 | 范围 | 结果 | 备注 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

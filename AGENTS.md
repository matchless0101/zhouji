# 粥记项目协作说明

## 项目定位

粥记是一款面向 iPhone 的极简任务、目标与投入时间记录工具。产品强调“打开即做、任务优先、真实胜于计划、温和但明确、克制扩展”。不要把它扩展成功能密集的任务管理系统、日历或情绪打卡产品。

## 目录结构

- `ios/`：iOS 应用、测试、Xcode 工程和 XcodeGen 配置。
- `ios/ZhouJi/`：应用源码。
- `ios/ZhouJiTests/`：单元测试。
- `ios/ZhouJiUITests/`：界面测试。
- `docs/`：产品总览、PRD 和实施路线图。
- `PRODUCT.md`：品牌、用户和设计原则摘要。

## 需求与文档优先级

1. 产品行为和验收标准以 `docs/02-产品需求文档-PRD.md` 为准。
2. 产品定位和长期原则参考 `docs/01-产品总览.md` 与 `PRODUCT.md`。
3. 技术实现顺序和版本范围参考 `docs/03-V1-V3-实施路线图.md`。
4. 文档有冲突时，不要自行扩大需求；指出冲突并向用户确认。
5. 完成影响用户行为的功能后，同步检查相关产品文档是否需要更新。

## iOS 技术约束

- 使用 Swift 6、SwiftUI 和 SwiftData，最低支持 iOS 17。
- 保持现有分层：`App`、`Core`、`Data`、`Domain`、`Features`。
- 新页面和组件优先沿用 `ios/ZhouJi/Core/Design/` 中的设计规范。
- 统计应从任务和计时事实聚合，不复制或回写累计统计数据。
- 删除任务或目标时，保留既有历史计时快照的语义。
- 没有明确需求时，不增加账户、后端、复杂分类、优先级或日程排布。
- 不要引入新的第三方依赖，除非用户明确同意。

## 工程与验证

`ios/project.yml` 是 XcodeGen 配置来源。新增或移动源码后，在 `ios/` 目录执行：

```bash
xcodegen generate
```

至少执行与改动直接相关的测试。涉及工程结构或跨模块修改时，执行模拟器构建：

```bash
xcodebuild -project ios/ZhouJi.xcodeproj \
  -scheme ZhouJi \
  -destination 'generic/platform=iOS Simulator' \
  CODE_SIGNING_ALLOWED=NO \
  build
```

若环境或模拟器服务阻止测试，明确区分环境问题与代码问题，并报告实际完成的验证。

## 修改原则

- 先阅读相关代码和产品规则，再修改。
- 保持改动聚焦，不顺手重构无关代码。
- 保留用户已有修改，不覆盖或回退未授权的变更。
- 修复缺陷时优先补充能够复现问题的测试。
- 每次功能迭代或新功能开发完成，并经过相关测试或构建验证确认没有问题后，必须创建一次 Git 提交；若仓库已配置远程地址，则同步推送到当前跟踪分支。
- 功能尚未完成、验证失败或存在已知问题时，不得以“完成”为名提交；应先修复问题，或明确向用户说明阻塞情况。
- 除上述“功能完成后提交并推送”的情形外，用户未明确要求时，不创建额外提交、不推送无关改动、不修改发布配置。
- 完成后用中文简要说明结果、关键文件和验证情况。
